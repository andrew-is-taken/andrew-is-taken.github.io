# app-ads.txt — IAB authorized sellers for the Subspace mobile game
#
# This is the authoritative copy. Ad exchanges crawl it from the root of the domain listed as the
# developer website in the Play Store listing — https://andrew-is-taken.github.io/app-ads.txt —
# which is exactly where this file lives.
#
# ── AdMob ─────────────────────────────────────────────────────────────────────────────────────
# Publisher ID from the App ID compiled into the build (ca-app-pub-4620809542635495~9845331315).
# f08c47fec0942fa0 is Google's certification id, the same for every publisher.
google.com, pub-4620809542635495, DIRECT, f08c47fec0942fa0

# ── Unity LevelPlay / Unity Ads and every mediated network ────────────────────────────────────
# TODO: account-specific, must be copied from the LevelPlay dashboard's own app-ads.txt generator
# (it produces the full block for every network in the waterfall) rather than guessed.
