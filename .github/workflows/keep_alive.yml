name: Keep Hugging Face Space Alive

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space Direct URL
        run: curl -L https://andrewsha-public-portfolio-bot.hf.space
