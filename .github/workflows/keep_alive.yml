name: Keep Hugging Face Spaces Alive

on:
  schedule:
    - cron: '*/15 * * * *' # Runs every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping New Portfolio Bot
        run: curl -L https://andrewsha-portfolio-bot.hf.space
